events {

}

http {
  include mime.types;
  sendfile on;
  server {
    server_name 206.12.122.169;

    location / {
      proxy_pass http://django:5000;
      proxy_redirect http://django:5000 http://$host;
      proxy_set_header Host $host;
    }

    location /lexmapr/static/ {
      root ~/lexmapr_django/lexmapr_django/static/;
    }

    location /lexmapr/media/ {
      root ~/lexmapr_django/lexmapr_django/media/;
    }

    listen 80;
  }
}
